<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        OFFLINE-FIRST ARCHITECTURE:
        This app is intentionally designed to work 100% offline.
        NO INTERNET PERMISSION is requested to ensure:
        - All data stays on the user's device (privacy-first)
        - App functions fully without network connectivity
        - No accidental network dependencies can be introduced

        DO NOT add android.permission.INTERNET to this manifest.
    -->

    <!-- SMS Permission for reading bank transaction messages (local-only operation) -->
    <uses-permission android:name="android.permission.READ_SMS" />

    <!-- Internet permissions for Google Drive API access -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Declare telephony feature as optional for ChromeOS/tablet compatibility -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:name=".KanakkuApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kanakku">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Kanakku">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            FileProvider for secure file sharing.

            Enables sharing exported transaction files (CSV, PDF) with other apps
            via content:// URIs instead of insecure file:// URIs.

            Key features:
            - Provides temporary read/write access to shared files
            - Prevents FileUriExposedException on Android 7.0+ (API 24+)
            - Supports Android share sheet integration
            - Paths configured in res/xml/file_paths.xml

            Authority: com.example.kanakku.fileprovider
            Usage: Used by ExportRepository.getFileUri() to generate shareable URIs
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.kanakku.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>